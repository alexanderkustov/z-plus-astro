---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="Z+ | Services">
  <section class="services-hero">
    <div class="hero-content">
      <h1 class="hero-title">
        Empowering <span class="highlight">Ambition</span>,<br />
        Building <span class="highlight">Futures</span>
      </h1>
      <p class="hero-lede">
        We provide tailored support for international talent and entrepreneurs
        to thrive in the UK market.
      </p>
    </div>
  </section>

  <section class="services-grid">
    <article class="service-card">
      <div class="icon-wrapper">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <path d="M12 20h9"></path>
          <path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"
          ></path>
        </svg>
      </div>
      <h2>Career Coaching</h2>
      <p>
        Programmes designed to help graduates and students secure jobs in
        international companies. The services are structured, tailor-made, and
        offered one-on-one.
      </p>
    </article>

    <article class="service-card">
      <div class="icon-wrapper">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path>
          <circle cx="9" cy="7" r="4"></circle>
          <path d="M23 21v-2a4 4 0 0 0-3-3.87"></path>
          <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
        </svg>
      </div>
      <h2>Start-up Networks</h2>
      <p>
        We provide students access to a large network of UK start-up and SME
        partners. Clients receive news and updates about internship programmes
        and advice on which opportunities best match their goals.
      </p>
    </article>

    <article class="service-card">
      <div class="icon-wrapper">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <line x1="12" y1="1" x2="12" y2="23"></line>
          <path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"></path>
        </svg>
      </div>
      <h2>Business Consulting</h2>
      <p>
        We offer information, understanding, and skills for international
        graduates and overseas entrepreneurs who wish to start a business in the
        UK. This support aims to help clients grow a successful business.
      </p>
    </article>
  </section>

  <section class="cta-section">
    <div class="cta-content">
      <h2>Ready to take the next step?</h2>
      <p>
        Whether you're looking for a career boost or starting a new venture,
        we're here to help.
      </p>
      <a href="mailto:info@z-plus.co.uk" class="button primary">Get in Touch</a>
    </div>
  </section>
</BaseLayout>

<style>
  .services-hero {
    padding: 2rem 0 4rem;
    text-align: center;
  }

  .hero-title {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .highlight {
    color: var(--accent-color);
    position: relative;
    display: inline-block;
  }

  /* Fun little underline effect for highlights */
  .highlight::after {
    content: "";
    position: absolute;
    bottom: 0.1em;
    left: 0;
    width: 100%;
    height: 0.2em;
    background: var(--accent-color);
    opacity: 0.2;
    z-index: -1;
    transform: skew(-10deg);
  }

  .hero-lede {
    font-size: 1.25rem;
    max-width: 600px;
    margin: 0 auto;
    color: var(--fg-color);
    opacity: 0.8;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 32px;
    margin-bottom: 96px;
  }

  .service-card {
    background: var(--bg-color);
    border: 2px solid var(--fg-color);
    border-radius: 24px;
    padding: 32px;
    box-shadow: 8px 8px 0 var(--fg-color);
    transition:
      transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1),
      box-shadow 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    height: 100%;
  }

  .service-card:hover {
    transform: translateY(-8px) translateZ(0);
    box-shadow: 12px 12px 0 var(--accent-color);
    border-color: var(--accent-color);
  }

  .icon-wrapper {
    width: 64px;
    height: 64px;
    background: var(--accent-soft);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 24px;
    color: var(--accent-color-hover);
  }

  .icon-wrapper svg {
    width: 32px;
    height: 32px;
  }

  .service-card h2 {
    font-size: 1.75rem;
    margin-bottom: 1rem;
    color: var(--fg-color);
  }

  .service-card p {
    margin-bottom: 0;
    line-height: 1.6;
    opacity: 0.9;
    flex-grow: 1;
  }

  .cta-section {
    background: var(--bg-color);
    padding: 64px 32px;
    border-radius: 32px;
    border: 2px dashed var(--fg-color);
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .cta-content {
    position: relative;
    z-index: 1;
    max-width: 600px;
    margin: 0 auto;
  }

  .cta-section h2 {
    margin-bottom: 16px;
  }

  .cta-section p {
    font-size: 1.1rem;
    margin-bottom: 32px;
    opacity: 0.8;
  }

  /* Decorative background element for CTA */
  .cta-section::before {
    content: "";
    position: absolute;
    top: -50%;
    left: -10%;
    width: 400px;
    height: 400px;
    background: var(--accent-color);
    opacity: 0.05;
    border-radius: 50%;
    filter: blur(80px);
  }

  @media (max-width: 768px) {
    .services-grid {
      grid-template-columns: 1fr;
    }

    .hero-title {
      font-size: 3rem;
    }
  }
</style>
